spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
    
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect

ethereum:
  network:
    name: sepolia-test
    chain-id: 11155111
    infura:
      project-id: test-project-id
      project-secret: test-project-secret
      endpoint: https://sepolia.infura.io/v3/test-project-id

logging:
  level:
    com.ethereum.wallet: DEBUG
